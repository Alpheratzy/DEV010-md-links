function isAbsolute(path){

    if(path === absolute){ // una ruta absoluta es cuando está toda la ruta de direccion al archivo. Cómo lo escribo.
        return true;
    } 
    else {
        return false;
    }
};

function convertToAbsolute(path){
// cuando sepa que es absoluto, resuelvo esta parte
};

function routeExistence(path){
    //cuando avance con las otras funciones, vengo a esta
}

module.export ={
    isAbsolute,
    convertToAbsolute,
    routeExistence
};